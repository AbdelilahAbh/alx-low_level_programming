#include "main.h"

/**
 * _strlen - swaps the values of two integers
 *
 * @s: parametre of function
 *
 * Return: void.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{}
	return (i);
}
